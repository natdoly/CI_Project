<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:int-file="http://www.springframework.org/schema/integration/file"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration
                           http://www.springframework.org/schema/integration/spring-integration.xsd
                           http://www.springframework.org/schema/integration/file
                           http://www.springframework.org/schema/integration/file/spring-integration-file.xsd">

    <!-- Définition du canal d'entrée qui reçoit des fichiers -->
    <int:channel id="fileInputChannel"/>

    <!-- Adapter qui surveille un répertoire pour les nouveaux fichiers JSON -->
    <int-file:inbound-channel-adapter id="fileReaderAdapter"
                                      channel="fileInputChannel"
                                      directory="dataIn"
                                      filename-pattern="*.json">
        <int:poller fixed-delay="2000"/>
    </int-file:inbound-channel-adapter>

    <!-- Service Activator qui appelle la méthode de traitement sur le fichier -->
    <int:service-activator input-channel="fileInputChannel"
                           output-channel="fileOutputChannel"
                           ref="articleService"
                           method="changeArticleDiscount"/>

    <!-- Définition du service qui contient la logique de traitement -->
    <bean id="articleService" class="service.ArticleService"/>

    <!-- Canal de sortie pour les articles transformés -->
    <int:channel id="fileOutputChannel"/>

    <!-- Exemple d'adaptateur de canal de sortie pour écrire des fichiers, si nécessaire pour une étape ultérieure -->
    <int-file:outbound-channel-adapter id="fileWriterAdapter"
                                       channel="fileOutputChannel"
                                       directory="dataOut"/>
</beans>
