<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:int="http://www.springframework.org/schema/integration"
    xmlns:int-file="http://www.springframework.org/schema/integration/file"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
        http://www.springframework.org/schema/integration/file http://www.springframework.org/schema/integration/file/spring-integration-file.xsd">

    <!-- Configuration des canaux -->
    <int:channel id="inputChannel" />
    <int:channel id="outputChannel" />

    <!-- Adaptateur de canal source pour lire les messages -->
    <int-file:inbound-channel-adapter directory="dataIn" channel="inputChannel" filename-pattern="articles.json">
        <int:poller fixed-delay="1000" />
    </int-file:inbound-channel-adapter>

    <!-- Composant pour filtrer les articles bleus -->
    <bean id="ArticleFilter" class="service.ArticleFilter" />

    <!-- Service activator pour filtrer les articles bleus -->
    <int:service-activator input-channel="inputChannel" output-channel="outputChannel"
                           ref="ArticleFilter" method="filterAndSaveBlueArticles" />

    <!-- Adaptateur de canal de sortie pour écrire les messages filtrés -->
    <int-file:outbound-channel-adapter directory="dataOut" channel="outputChannel" />

</beans>